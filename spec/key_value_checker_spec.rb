require "spec_helper"

describe KeyValueChecker do
  it "has a version number" do
    expect(KeyValueChecker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
